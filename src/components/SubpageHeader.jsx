import React from "react";

export default function SubpageHeader({ scrollToTop = false }) {
  // This component is now deprecated - back button is integrated into Navigation
  // Keeping the component for backward compatibility but it renders nothing
  return null;
}
